{
  "name": "mercado-libre-api",
  "version": "1.0.0",
  "description": "API for the ML test",
  "main": "index.js",
  "author": "Wencel Santos",
  "scripts": {
    "start": "node --optimize_for_size --max_old_space_size=920 --gc_interval=100 src/index.js",
    "dev": "nodemon src/index.js",
    "debug": "node inspect src/index.js",
    "test": "jest --watchAll --runInBand"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "engines": {
    "node": "14.x"
  }
}
